<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EmpireGeneral.Styles.styleFrame">

    <!--Frame-->
    <Style x:Key="rlCard" TargetType="Frame">
        <Setter Property="BackgroundColor" Value="{DynamicResource bgPageColor}"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Margin" Value="2"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="HasShadow" Value="{OnPlatform iOS=false, Android=true}"/>
    </Style>

    <Style x:Key="rlFrame" TargetType="Frame">
        <Setter Property="BackgroundColor" Value="{DynamicResource bgPageColor}"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Margin" Value="2"/>
        <Setter Property="HasShadow" Value="{OnPlatform iOS=false, Android=true}"/>
    </Style>
    
</ResourceDictionary>